# Agent 开发学习笔记

> 记录格式:**每条先写「现象」(我看到了什么),再写「规则」(所以约束是什么)**。
> 只写现象是背下来的,写出规则才算理解——面试追问第二层时,区别就在这里。

---

## 一、环境与配置

### 1. 环境变量会静默盖掉 .env

**现象**:`.env` 里写了 `ANTHROPIC_BASE_URL=https://relay.example.com`(一个第三方中转),请求却打到了 `api.anthropic.com`,报的是 Anthropic 官方格式的 401,不是中转站的错误格式。

**规则**:`load_dotenv()` **默认不覆盖已存在的环境变量**,优先级是「进程环境 > .env 文件」。这类问题没有任何报错,只是悄悄用了另一个值。排查配置问题时**先打印进程里实际生效的值**,不要相信配置文件。需要强制覆盖时用 `load_dotenv(find_dotenv(), override=True)`。

**识别技巧**:错误信息的**格式**能告诉你请求实际到了哪。中转站返回它自有的错误类型(形如 `<vendor>_api_error`),官方返回 `authentication_error` + `req_011...` 格式的 request id。

### 2. 凭证变量的优先级陷阱

**现象**:`.env` 模板里留着 `ANTHROPIC_API_KEY=your_key_here` 占位符,同时填了 `ANTHROPIC_AUTH_TOKEN`,认证失败。

**规则**:Anthropic SDK 凭证解析顺序是 `ANTHROPIC_API_KEY` → `ANTHROPIC_AUTH_TOKEN` → OAuth profile,**先匹配先用**。而且:
- 占位符也算「已设置」,会抢先生效
- **空字符串也占位**,`ANTHROPIC_API_KEY=""` 照样赢
- 两个都设会同时发 `x-api-key` 和 `Authorization` 两个 header,服务端直接拒

用哪个就**只留哪个**,另一个整行删掉,不是留空。

### 3. 一个 key 驱动多个 SDK

**规则**:OpenAI SDK 自动读 `OPENAI_API_KEY` + `OPENAI_BASE_URL`,所以把 base_url 指向任何 OpenAI 兼容服务(DeepSeek 等),**客户端构造代码一行都不用改**。LiteLLM 用 `openai/<model>` 前缀时也复用同一份环境变量。

### 4. 中转站的额外约束

**现象**:同一个令牌,教程 01/03/04 都能跑,到 05 突然 403 `This API endpoint is only accessible via the official Claude CLI`。

**规则**:中转服务有自己的令牌分组和路由策略(如 CC 分组只允许 Claude Code 访问),**和官方 API 的行为不完全一致**。用中转时,非代码原因的失败要先怀疑服务端策略。

---

## 二、协议层:模型到底返回什么

### 5. content 是类型化数组,永远不要按下标取

**现象**:`response.content[0].text` 报 `AttributeError: 'ThinkingBlock' object has no attribute 'text'`。

**规则**:`content` 是**类型化内容块的数组**,索引位置**不做任何保证**——可能是 `thinking`、`text`、`tool_use`,可能有多个 text 块,也可能一个都没有。**永远遍历 + 按 `block.type` 筛选**。

注意 thinking 块只是**触发发现的具体案例**,不是规则本身。按「第一个是 thinking」去理解,下次会写 `content[1]` 再崩一次。

**相关**:Claude Sonnet 5 / Opus 5 **不传 `thinking` 参数时默认开启自适应思考**,所以 content 里常有 thinking 块。而 `display` 默认 `"omitted"`,块存在但文字为空——**肉眼看不出异常,一索引就炸**。

### 6. 往历史里追加要原样,往用户返回才筛选

**规则**:两个方向不对称,很容易搞混:
- `messages.append({"role": "assistant", "content": response.content})` —— **整个数组原样追加**,包括 thinking 块。同一模型继续对话时 thinking 块必须原封不动传回,自己筛选或重构会 400
- 返回给用户展示时才做 type 过滤

### 7. stop_reason 是枚举,必须穷举

**现象**:教程 05 只判断了 `end_turn`,遇到 `max_tokens` 时会:不满足退出条件 → 遍历 content 找不到 tool_use → `tool_results` 是空列表 → 追加 `{"role":"user","content":[]}` → 下次调用带空消息 → **400 崩溃**。

**规则**:`stop_reason` 取值有 `end_turn` / `tool_use` / `max_tokens` / `refusal` / `pause_turn`。**每个都要有明确分支,并且要有 `else` 兜底**——枚举以后加了新值,你得到的是一条清楚的信息,而不是诡异的崩溃。

`stop_reason` 同时是 **agent loop 的退出开关**。

---

## 三、Agent Loop

### 8. 角色分配(字节面试原题)

**规则**:
- 模型发起的工具调用 → **`assistant` 角色**追加
- 工具执行结果 → **`user` 角色**追加

**为什么**:Messages API 要求 user / assistant **严格交替**。模型的工具调用是它的**输出**,占 assistant 位;工具结果是喂回模型的**输入**,占 user 位。

只说角色是背的,加上「为什么」才是懂的。

**⚠️ 2026-08-23 修正:上面这条只在 Anthropic Messages API 下成立。**

原文写的是「不是约定俗成,是对话结构决定的」——**这句是错的**。
OpenAI 形状(DeepSeek 同)有一个**专门的 `tool` 角色**,一个工具调用一条消息,
既不占 user 位,也没有严格交替的要求:

```python
# Anthropic:所有结果收进一条 user 消息的 content 数组
{"role": "user", "content": [{"type": "tool_result", "tool_use_id": ..., "content": ...}, ...]}

# OpenAI / DeepSeek:每个调用一条独立消息
{"role": "tool", "tool_call_id": ..., "content": ...}
```

所以它**恰恰就是**约定俗成——是 Anthropic 的约定,不是对话结构的必然。

**修正后的规则**:角色分配是**厂商协议层的约定**,不是对话结构的推论。
答这道题要先问「哪家的 API」,再给答案;两家都能说出来才算真懂。

**怎么发现的**:写 `harness/provider.py` 时决定「不包装 response、直接透传 SDK 对象」,
追问这个取舍的代价时才发现——代价不在适配层,在 `loop.py`:
`collect_text`、三个 `stop_reason` 分支、`block.type` 判断、`handle_tool_use` 造的 dict,
换成 DeepSeek 全要改。**顺着「换 vendor 要改哪些行」倒推,才暴露出这条笔记超了范围。**

这就是第 38 条那个毛病的又一例:**只测过一家,却写成了协议层的普遍规律。**

### 9. tool_result 必须一一配对,拒绝也要返回

**规则**:每个 `tool_use` 块**必须**有一个 `tool_use_id` 匹配的 `tool_result`,少一个下次调用就 400。所以:
- 用户拒绝执行 → 返回 `is_error: True` 的结果,不是什么都不返回
- 命令被护栏拦下 → 同样返回 `is_error` 结果

好处是模型知道被拒了、可以换方案,而不是莫名其妙断掉。

### 10. 一轮的所有结果收进一条消息

**现象**:把 `messages.append(...)` 写在 per-block 循环里,一轮请求 2 个工具就会追加 2 条 user 消息。

**规则**:一轮里所有 `tool_result` 收进**一个列表**,循环结束后追加成**一条** user 消息。拆成多条不仅破坏交替,还会**让模型逐渐不再做并行调用**。

**⚠️ 2026-08-23 修正:作用域同第 8 条,这条只对 Anthropic 成立。**
OpenAI 形状下「一个调用一条 `tool` 消息」是**正确写法**,不是 bug,那边也没有严格交替的约束。
「并行调用会退化」的观察是在 Anthropic 上测的,**没有在 OpenAI 形状上验证过**。(证据强度:单厂商实测)

### 11. while True 没有上限 = 死循环

**规则**:agent loop 必须有步数上限,并且预算耗尽时**诚实返回失败**(`"reached the N-step limit"`),不能假装成功。这是「生产五大坑」的第一条。

### 12. 工具调用间的旁白会被丢掉

**现象**:agent 干活时终端一片安静,只有 `-> Tool: bash(...)`。

**规则**:模型在决定调工具的那一轮**经常同时输出文字**("我先看一下这个文件…"),这段文字在 `content` 里,但只捞 `tool_use` 的循环会把它丢掉。要不要展示是**设计选择**(inter-tool-call narration),但要知道它存在、是你主动丢的。

### 13. 工具粒度:模型能推出来的,别做成工具

**现象**:让 agent「列出目录文件并数出几个」,预期两次工具调用,实际只有一次——模型跑完 `ls -la` 后**自己从输出里数出来了**。

**规则**:**该成为工具的,是模型做不到或做不可靠的事**——访问外部状态、执行副作用、精确计算。模型能从上一步结果推出来的,做成工具反而多一轮往返、多一次延迟、多一份 token。

---

## 四、安全护栏

### 14. 子串黑名单同时误伤和漏网

**现象**(实测):

| 命令 | 黑名单结果 | 问题 |
|--|--|--|
| `echo format` | 拦截 | `"rm"` 是 `"fo**rm**at"` 的子串,**误伤** |
| `mv a.txt /dev/null` | 放行 | 破坏性操作,**漏网** |
| `python3 -c "..."` | 放行 | 任意代码执行,一句话绕过全部黑名单 |

**规则**:黑名单在安全上约等于零。正确做法是**三层独立防御**:

```
第 1 层  拒 shell 操作符   →  保证「只有一条命令」的前提成立
第 2 层  可执行文件白名单  →  前提成立后,回答「这条命令允许吗」
第 3 层  shell=False       →  即使前两层被绕过,也没有 shell 可供利用
```

**为什么第 1 层必须在第 2 层之前**(不是并列):白名单回答的是「哪个程序允许运行」,它隐含前提是**字符串里只有一个程序**。shell 语法让这个前提不成立——`echo $(whoami)` 里 `$(...)` 是**命令替换,在主命令之前执行**,你检查第一个词是 `echo` 就放行了,而 `whoami` 早跑完了。同类的还有 `;` `&&` `||` `|` `` ` `` 和换行符。

> **白名单问的是「哪个程序」,但 shell 语法让一个字符串能调起多个程序。必须先保证只有一条命令,这个问题才有意义。**

**shell=True 到底做了什么**:`subprocess.run(s, shell=True)` 实际执行 `/bin/sh -c "<s>"`,把你的字符串当成「shell 语言写的程序」去解析。而 `shell=False` + 列表直接调 `execvp`,第一个元素是程序、其余是 argv,**没有解析步骤**,元字符只是普通字符。

实测对比:

| 输入字符串 | `shell=True` 输出 | `shell=False` 输出 |
|--|--|--|
| `echo hello; echo 注入` | `hello` + `注入`(**跑了两条命令**) | `hello; echo 注入`(字面量) |
| `echo $(whoami)` | 打印出真实用户名(**信息泄露**) | `$(whoami)`(字面量) |
| `echo a && echo b` | `a` + `b` | `a && echo b`(字面量) |

**「消灭」和「过滤」的区别**(最值得记的一点,和黑名单是同一个道理):

| 做法 | 本质 | 失败模式 |
|--|--|--|
| `shell=True` + 过滤元字符 | **黑名单** —— 跟 shell 语法比赛 | shell 有 `$IFS`、`${x:-...}`、反斜杠转义、嵌套引号、`$'\x41'`……检查表迟早漏一个 |
| `shell=False` | **移除能力** | 这类攻击**不存在**,不是"拦住了" |

> **一般规则:能移除能力就不要过滤输入。过滤是黑名单,黑名单终会被绕过。**

**既然 shell=False 已安全,为什么还留第 1 层**:① 给模型清晰的拒绝理由(否则它看到的是 `ls: cannot access '&&'` 这种莫名报错,不知道怎么改);② 纵深防御,防止以后有人把 `shell=True` 改回去;③ 出现操作符本身是个意图信号。

### 15. 护栏可以在代码里,也可以在人那里

**现象**:教程 04 把护栏放代码里(黑名单,实测无效),教程 05 把护栏完全交给人(执行前 `Approve? (y/n)`)。

**规则**:这是真实的架构选择,各有代价。生产里通常两者都要:**代码挡住明确危险的,人审批不可逆的**。

---

## 五、成本与上下文

### 16. API 无状态,记忆是你重发出来的

**规则**:服务端没有对话记忆。所谓「记忆」完全是客户端维护一个 messages 数组、**每轮把全部历史重新发一遍**。这是阶段 05(上下文工程)和阶段 08(记忆系统)存在的全部起因。

### 17. 成本随轮数平方增长

**现象**(实测):history length 1 → 3 → 5 → 7,每轮 input tokens 递增。

**规则**:第 N 轮的 input tokens ≈ 前面所有内容之和,所以**单轮成本 O(N),整场累计 O(N²)**。聊 10 轮和 50 轮不是 5 倍关系,是 25 倍。

三个直接推论:
- 历史无限增长会撑爆上下文窗口 → **为什么需要 compaction**
- 成本平方增长 → token 预算和成本核算的起点
- 重发内容大部分每轮相同 → **prompt caching 能省 90% 的原理,前缀不变才能命中**

**面试用**:「Agent 落地最大瓶颈」里成本那一条,技术根源就在这里。从 O(N²) 讲起比说「token 消耗大」强得多。

### 18. 带工具的一轮吃 4 条消息

**规则**:普通一轮 +2 条(user + assistant),**带一次工具调用的一轮 +4 条**(user + assistant工具调用 + user工具结果 + assistant最终回答)。工具调用越多历史膨胀越快——**阶段 05 要压缩的主要就是这堆工具结果**。

### 19. system prompt 在前缀最前面

**规则**:system prompt 是独立字段,每轮随请求发送,且**位于前缀最前面**。它一变,整个缓存全废。这是上下文工程的高频考点。

---

## 六、方法论

### 20. 跑通 → 重写 → 读透

**规则**:顺序不能反。先跑通形成直觉,再关掉源文件**凭理解重写**,最后读官方文章对账。

**跟练不重写等于没学。** 重写要趁代码还小的时候做(60 行左右最合适),再往后体量就大到重写不动了。

### 21. 先读代码再读文档

**规则**:先读文档会直接接管它的框架,然后就不会注意到东西了。**先读代码再读文档,文档就从「讲课」变成「对答案」——你在哪里想错了,哪里才是真正学到的地方。**

### 22. 「知道现象」和「说出规则」差一层

**现象**:自检三个问题时,三条答案都是「我看到了什么」,说不出「规则是什么」。

**规则**:面试官追问两层就能分辨真懂和背过,**掉下来的位置就是第二层**。每踩一个坑强迫自己写两句话:一句现象,一句规则。本文件的格式就是为此设计的。

---

## 七、RAG 与检索

### 23. 语料污染:73% 的 chunk 是垃圾

**现象**:索引 `01-foundations` 后,539 个 chunk 里 **391 个(73%)来自 `06-codebase-navigator/repos/openai-swarm/`** —— 一个之前 clone 进来的第三方仓库,里面是客服文章的 JSON。`SKIP_DIRS` 有 `node_modules`、`.venv`,但没有 `repos`。

**规则**:索引一个本地目录会**递归扫进它的缓存和下载产物**。除了依赖目录,还要排除:clone 下来的第三方仓库、向量库自身的持久化目录、任何生成物。**索引前先看 chunk 的来源分布**,一行 Counter 就能发现问题。

清掉后 recall@5 从 43% → 54%,recall@10 从 50% → 64%。**一行 `SKIP_DIRS` 改动。**

### 24. 检索强弱项由语料决定,不由检索方式决定

**现象**:污染状态下,`exact_identifier` 类 recall@5 = **100%**,而 `semantic` 类 = **0%**。这和"向量检索擅长语义、不擅长精确匹配"的直觉**完全相反**。

看未命中项的 top-1 就懂了:中文自然语言查询全部匹配到了那些**中文客服文章 JSON**。它们语义上确实"更像"一个中文问句,把真正的代码 chunk 挤出了 top-10。而 `def check_command(` 这种**罕见且独特的 token 序列**,那些文章在这个方向上不构成竞争。

**规则**:**离开具体语料谈「向量检索擅长什么」没有意义。** 同一套检索器换个语料,强弱项会整个翻转。真正决定命中的不是"查询类型 vs 检索方式"的匹配度,而是**语料里有什么在跟你竞争**。

同一机制观察到两次:第二次是我自己写的评测脚本(里面全是中文 print 字符串)在一堆英文代码里成了新的污染源。

### 25. 大块会稀释语义(已验证);"小块不好"曾被我误判(已修正)

**现象**:embedding 把**任意长度**的 chunk 压成**一个固定长度向量**(MiniLM 是 384 维)。`my_agent.py` 的第 1 个 chunk 塞了 docstring + import + 4 个常量 + TOOLS schema 共 1094 字符,一个向量要代表七件事,离哪件都不够近。

**受控实测**(同一查询,只改被比较的文本长度):

| 查询 | vs 整个 chunk(1094 字符) | vs 只有那几行常量 |
|--|--:|--:|
| `SHELL_OPERATORS 包含哪些操作符` | 0.2670 | **0.5724** |
| `ALLOWED_COMMANDS 白名单里有哪些命令` | 0.2169 | **0.3969** |

**规则:大块稀释是真的。** 把常量拆成独立 chunk 后,`value_lookup` 类 recall@5 从 **67% → 100%**,而且 @1/@3 提升幅度(+15/+17)大于 @5——不只是"能找到",是**排得更靠前**。

---

**⚠️ 本条的一个错误已修正。** 原先我还写了一条"chunk 太小 → 失去上下文",证据是:

```
查询「agent 循环最多跑多少步」
  vs 整个 chunk    0.3533
  vs MAX_STEPS = 10  0.1943  ← 看起来小块更差
```

**这个证据是坏的**:它用的是**中文**查询,而对照实验后来证明中文查英文语料有巨大的跨语言损失(见第 35 条)。换成英文查询 `what is the maximum number of agent loop steps` 再测,那个 **14 字符**的 `MAX_STEPS = 10` 块 **排名第 1**(distance 0.4450 ≈ 相似度 0.555,而不是 0.194)。

**所以那 0.194 测的不是"块太小",是"中文查英文"。变量没控住。**

**当前诚实的结论**:大块稀释有受控证据支持;**"块小到什么程度会开始变差"我没有测过,不知道**。不要把没验证的下限当成规则用。

**下一步该验的**:给每块加文件路径/所属类的前缀元信息,看能不能进一步提升——这个假设仍然合理,但同样需要受控实验,不能靠推理。

### 26. chunk 里含有答案 ≠ 能被检索到

**现象**:问 `BLOCKED_COMMANDS 的值`,模型编了一个 6 元素的列表(真实是 10 个),还在上一轮断言 `dd` 不在黑名单里(实际在)。而含完整定义的 chunk **存在且格式良好**(874 字符,行 103-131)。

**规则**:检索比的是**向量余弦相似度**,和文本里有没有那个字符串**毫无关系**。所以"答案在语料里"完全不保证"答案能被捞出来"。这也是 RAG 最危险的失败模式——**不是答不出来,是编一个格式漂亮、语气笃定的错答案。**

---

## 八、评测方法论

### 27. 数据泄漏:答案键进了语料

**现象**:把 `eval/eval_set.json` 建在语料目录内并重建索引后,recall@5 从 43% 跳到 **64%**。实测发现 `.json` 在可索引扩展名里,`eval_set.json` 被切成 5 个 chunk,而 **36 条 ground truth 字符串里有 33 条(92%)原样写在这个文件里**。检索命中的是答案键本身。移出语料后真实值是 54%。

**规则**:**答案键必须和语料物理隔离——放到语料目录外面,不是加 `SKIP_DIRS`。** 配置会被改回去,目录结构是约束。

这是评测最经典的翻车方式,危险之处在于它给出的是**一个令人鼓舞且合理的数字**。64% 一点都不离谱,不反常就不会去查。

### 28. 提升"好得不太真实"时,先查泄漏再庆祝

**规则**:任何一个小改动带来超预期的提升,第一反应应该是找泄漏,不是记录成果。这条比技术细节更重要——**带着一个泄漏出来的数字去面试,追问一层就穿帮。**

### 29. 预测与结果不一致,是发现 bug 最可靠的信号

**现象**:我预测移走 `eval/` 会让分数**上升**(能救回 4 个 MISS),结果**下降**了。正是这个矛盾逼出了泄漏的排查。

**规则**:**每次改动前先写下你预期数字往哪边动、大概多少。** 不写的话,数字怎么变你都能事后编一个解释,bug 就永远藏着。

### 30. 先评你能测准的那一层

**规则**:评检索层(recall@k)是**确定性的、免费的**——同一 query 永远返回同一批 chunk,不调 LLM。评端到端答案需要 LLM judge,贵、慢、有随机性。**先把能测准的层测准,把问题隔离在你正要修的那一层。**

### 31. ground truth 用字符串,不用行号

**规则**:行号随代码改动漂移,评测集第二天就失效。用**独一无二的字符串**(如 `BLOCKED_COMMANDS = [`)判断命中,稳定得多。

### 32. 评测集必须分类,否则数字不能指导行动

**现象**:整体 recall@5 = 54% 什么都说明不了。拆开是 `exact 100% / value 33% / semantic 25% / cross 0%` —— 立刻知道该修哪里。

**规则**:**单一总分是用来汇报的,分类拆分才是用来干活的。**

### 33. 评测集本身要能自检

**现象**:清掉污染后,校验器报 `q26: 语料里找不到 'def embed_chunks('` —— 真实函数名是 `index_chunks`。这个错之前没暴露,因为**被污染的语料里恰好有第三方文件含这个字符串**。

**规则**:跑评测前先校验每条 ground truth 在语料中确实存在。**ground truth 错了的评测集,产出的数字毫无意义,而且错得悄无声息。**

### 34. 一次只改一个变量

**现象**:我在同一轮里既移走了 `eval/` 又修了 q26 的 ground truth,违反了自己定的规则。这次因为泄漏影响够大还能定位,换个场景就说不清是哪一处的作用。

**规则**:改 → 测 → 记数字 → 再改下一处。**否则你得到的是一堆无法归因的数字。**

---

## 九、跨语言检索

### 35. 查询语言和语料语言不一致,是被严重低估的瓶颈

**现象**:同一语料、同一 ground truth,只把 28 条查询从中文换成英文:

| 类别 (recall@5) | 中文查询 | 英文查询 | Δ |
|--|--:|--:|--:|
| `exact_identifier` | 100% | 100% | **±0** |
| `semantic` | 25% | **100%** | **+75** |
| `value_lookup` | 33% | 67% | +33 |
| `cross_file` | 0% | 33% | +33 |
| **整体** | 54% | **86%** | **+32** |

**规则**:`all-MiniLM-L6-v2` 是英文模型,语料是英文代码+英文注释。中文查询要跨语言对齐,损失巨大。

**对照组证实了机制**:`exact_identifier` 纹丝不动——因为 `chunk_python`、`VectorStore` 这类查询的关键 token 中英文版本**本来就一样**,没有跨语言损失。而整句自然语言查询损失最大。

**生产上两条修法**:① 换多语言 embedding(`BGE-M3` / `multilingual-e5` / `paraphrase-multilingual-*`),根治但要重建索引;② **查询改写**——检索前把用户问题翻译成语料语言再 embed,便宜且不用重建索引。常常两个都用。

### 36. 优化某一层之前,先确认瓶颈真的在那一层

**现象**:上一步我已经规划好要改切块(AST 切块、上下文前缀、常量单独成块),还实测了稀释效应的相似度数字。**结果 `semantic` 的 0%/25% 根本不是切块问题,是跨语言。** 改一百遍切块也救不回来。

**规则**:稀释效应是真的、测出来的数字是真的,**但它不是当时最大的瓶颈**。有评测集才能分清「真实存在的问题」和「当前最该修的问题」——两者经常不是同一个。

评测集到此已经三次证明自己:抓出语料污染 → 抓出数据泄漏 → 拦下一个方向性错误的优化。

### 37. 小评测集上,边界附近的单条变化是噪声

**现象**:中英文对照里有 4 条"变差"(如 `9→MISS`、`3→4`),但类别级别是压倒性改善。

**规则**:n=28 时,原本就在 rank 8-10 徘徊的条目,在 k=5/10 边界上抖动属于噪声。**要看类别级别的系统性移动**(semantic +75% 这种),不要对个别条目的排名变化编故事。

---

## 十、推理纪律

### 38. 最容易犯的错:规律超出了数据能支撑的范围

**现象**:同一段学习里,我连续犯了两次同类错误。

| 次 | 我有的真实数据 | 我推出的规律 | 错在哪 |
|--|--|--|--|
| 1 | 常量块的稀释效应(受控实测) | "块变小 → `semantic` 类会降" | **作用范围**——语义查询根本不检索常量块,那些 chunk 一个字节都没变 |
| 2 | 中文查询 vs 大块/小块的相似度 | "块太小 → 失去上下文" | **变量没控住**——差异来自查询语言,不是块大小 |

**两次的形状一模一样:真实数据 → 提炼出一条听起来对的规律 → 把规律用到了数据管不着的地方。**

**规则**:每次从数据得出规律时,追加两个问题——

1. **这条规律的作用范围是什么?** 我要应用它的对象,真的在这个范围里吗?
2. **产生这个数据时,除了我关心的变量,还有什么在变?**

**数据越硬,越容易跳过这两问**。0.2670 vs 0.5724 这种精确数字给人一种"已经验证过了"的错觉,但验证的只是那**一个特定对比**,不是你从它引申出去的整个论断。

**推论**:笔记里的规则要标注**证据强度**——哪些是受控实验得出的,哪些是推理得出的。第 25 条现在就是这么写的:稀释有受控证据,而"小块下限"明确标注为**未测**。**未经验证的推论混在验证过的结论里,比没写更危险。**

---

## 十一、模块边界

### 39. 「目录独立」不等于「能独立运行」

**现象**:`rag-eval/` 的 4 个文件放在一个单独目录里,一直以为它是自包含的。真要把它抽成独立仓库时才发现跑不起来:

- `run_eval.py` 靠 `sys.path.insert` 指向兄弟目录的 `06-codebase-navigator`,才能 import `indexer` / `store`
- `eval_set.json` 的 `corpus` 字段写死了绝对路径 `/Users/<用户名>/...`

**规则**:判断一个模块能不能被搬走,看的是**它的外部依赖是否显式且可配置**,不是看它的文件放在哪个目录。绝对路径和 `sys.path` 魔改是两种最隐蔽的耦合——**它们在原机器上永远不报错**,只在换机器、换目录时才炸,而那通常是你最不想调试的时刻(交付、演示、面试官 clone 下来跑)。

把隐式依赖变显式的标准做法:外部路径走**环境变量 + 合理默认值 + 找不到时的明确报错**,数据文件里只存**相对路径**,由代码拼绝对路径。

**证据强度**:直接观察(抽仓库时实际踩到)。修法已实测——改成 `AAE_REPO` 环境变量 + 相对 `corpus` 后复跑,结果与原位置**完全一致**(中文 recall@5 = 57%,英文 93%),说明是纯粹的解耦,没有改变行为。

---

## 十二、指标设计

### 40. 一个字段承担两种语义,指标一定算错

**现象**:评测集里 `expect_contains` 这一个字段,同时被我用成了两种意思:

```jsonc
// q18 —— 「任选其一」, 两种写法都对
"expect_contains": ["stop_reason == \"end_turn\"", "stop_reason != \"tool_use\""]

// q26 —— 「全部都要」, 问的是完整管线经过哪些模块
"expect_contains": ["def chunk_repository(", "def index_chunks(", "def add_chunks("]
```

**规则**:**「或」和「与」共用一个字段名,任何基于它的聚合指标都是错的**,而且错得没有报错。
拆成 `expect_any`(命中任一 → hit@k)和 `expect_all`(全部必需 → coverage@k)之后,
指标才有定义。**先让 schema 承载语义,再谈算什么分**——用类别名(`category`)去推该用哪个指标
是错的,类别是给人看的分组,不是判定依据。

### 41. 指标改对了,数字不一定会动

**现象**:把 `cross_file` 从 hit@k 换成 coverage@k 之前,我预测数字会下降,理由是
「命中 1/3 被记为满分」属于虚高。实测:

| | 改之前 (hit) | 改之后 (coverage) | |
|--|--:|--:|--|
| 中文 `cross_file` @10 | 33% | **11%** | 降了 |
| 英文 `cross_file` @5 | 33% | **33%** | **没动** |

中文降了,是因为 q28 只捞回 3 个期望块里的 1 个。英文没动,是因为唯一命中的 q27
**本来就是 2/2 全覆盖**——没有水分可挤。

**规则**:**「这个指标有缺陷」和「这个数字受了缺陷影响」是两件事。**
hit@k 相对 coverage@k 的**高估上限**是确定的(逐条 coverage ≤ hit,数学约束),
但**实际高估了多少完全取决于数据**。命中的查询若本来就全覆盖,两个指标给出同一个数。

修完测量方法数字不动,**不代表白改**:改之前那个 33% 是「碰巧对」,改之后是「可信地对」。
**指标的价值在于它下次还对,不在于这次让数字变了多少。**

**我错在哪**(和第 38 条同一个形状):我知道 `coverage ≤ hit` 必然成立,就直接推出「所以会降」。
中间少了一步——降不降取决于**命中的那条查询是不是部分命中**,那是个要查数据才知道的经验问题,
我没查就当成已知了。诚实的预测应该写成条件式:**「若命中的查询是部分覆盖则降,若已全覆盖则不动」**。

**证据强度**:受控实验。只改了指标定义,语料、索引、查询全部没动;
其余三个类别的 12 个数字**逐格未变**(`exact 64/100/100/100`、`semantic 12/12/25/25`、
`value_lookup 50/50/50/67`),证明改动没有外溢——**这条「作用范围」自检比分数本身更值钱**,
它排除了「我把公共代码路径改坏了」这个可能。

### 42. 文件路径前缀:假设被证伪,而且证伪得有信息量

**现象**(受控实验,只改切块器一处):给每个 chunk 前面加一行 `# file: <相对路径>`。

英文查询 @5:

| 类别 | 加前缀前 | 加前缀后 | Δ |
|--|--:|--:|--:|
| `exact_identifier` | 100% | 91% | **-9** |
| `semantic` | 100% | 88% | **-12** |
| `value_lookup` | 100% | 100% | ±0 |
| `cross_file` (coverage) | 33% | **17%** | **-16** |
| **整体** | **93%** | **84%** | **-9** |

中文查询 @5:

| 类别 | 加前缀前 | 加前缀后 | Δ |
|--|--:|--:|--:|
| `value_lookup` | 50% | **83%** | **+33** |
| 其余三类 | — | — | 全部 ±0 |
| **整体** | **57%** | **64%** | **+7** |

**它要救的 `cross_file` 反而更差了**(英文 33% → 17%,q27 从 2/2 掉到 1/2),中文纹丝不动 0%。

**规则**:前缀是一笔**加在每一块上的固定成本**,净效果取决于
**「前缀比原内容更好匹配,还是更差」**:

- 原内容已经能被精确匹配(英文查 `CHUNK_SIZE = 50`)→ 前缀只是噪声,**稀释**
- 原内容几乎没有可匹配文本(中文查一个 14 字符的常量块)→ 前缀是**净新增信号**

所以受影响最大的确实是**短块**,但**方向不固定**。同一批 chunk、同一个改动,
中文 `value_lookup` +33、英文 `value_lookup` ±0——**唯一的变量是查询语言**。

**我错在哪(两处)**:

1. 我预测「小块被前缀主导 → `value_lookup` 有下降风险」。**「影响最大的是小块」对了,方向反了。**
   我把第 25 条的稀释效应直接搬过来,却漏了它的前提:**稀释的前提是原内容本身是有效信号**。
   14 字符的常量块配中文查询,原本就匹配不上——被"稀释"掉的是零。
2. 我预测路径是英文词、会帮英文多于帮中文、**中英差距拉大**。实际相反:英文 -9、中文 +7,
   **差距缩小**。因为英文侧三类已经 100%,**没有上升空间,只有下跌空间**——
   这一点我在预测里明明写了,却没把它用到这条推论上。

**证据强度**:受控实验,做了两次(见下一条)。改动已回滚,基线逐格恢复。
被否决的补丁留在 `rag-eval/patches/path-prefix-rejected.patch`。

### 43. 改被索引的代码 = 同时改了语料

**现象**:实现前缀功能时,我把逻辑写成一个新的顶层函数 `add_path_prefix`。
重建索引后 chunk 数从 213 变成 **214**——**加前缀不该改变块数**。

原因:`chunker.py` 自己就在被索引的语料里(`06-codebase-navigator/indexer/chunker.py`),
新增一个顶层 `def` 就等于给语料新增一个 chunk。实测那个新块 **6 次进入 top-10**
(q26 英文 rank 10、q21/q22 英文、q10/q11),而英文退步的正是这几类。

**规则**:**当工具本身在它处理的语料里,任何对工具的修改都是对语料的修改。**
这是第 27 条(答案键不能进语料)的同一个形状,只是这次混进语料的是**代码改动**而不是答案键。

对策不是「别改」,是**让改动尽量不产生新的切块边界**:把逻辑内联进已有函数,
块数不变,语料扰动降到最小。改完重跑,213 块,**所有数字与污染版逐格相同**。

**这次是运气**:复跑证明那 6 次侵入没改变任何一条得分。但**这是事后才知道的**——
当时我无法区分「前缀导致英文退步」和「新增竞争块导致英文退步」。
**混杂变量惰性与否要靠实验确认,不能靠猜**;chunk 数对不上就是它在报警。

**证据强度**:直接观察(chunk 计数 213→214)+ 受控复跑(内联后 213,结果逐格一致)。

---

## 十三、样本量

### 44. 小样本上的 100% 是假象,不是成绩

**现象**:把评测集从 28 条扩到 60 条,**检索系统一个字都没改**——语料、索引、检索代码、
指标定义全部不动,只有查询集变了。英文 @5:

| 类别 | 28 条 | 60 条 | n 变化 |
|--|--:|--:|--|
| `exact_identifier` | 100% | 100% | 11 → 15 |
| `semantic` | 100% | **81%** | 8 → 16 |
| `value_lookup` | 100% | **79%** | 6 → 14 |
| `cross_file` | 33% | **46%** | 3 → 15 |
| **整体** | **93%** | **76%** | 28 → 60 |

**规则**:**样本量不足时,分数的误差比你想测的效应还大。**

- 那三个 100% 是假象:n=8 和 n=6 时,「全对」只需要连续 8 次和 6 次不出错
- 而 `cross_file` 的 33% 是**被低估**的:n=3 时一条查询就是 33 个百分点,
  那个 33% 是「三选一命中」的运气

**误差是双向的。** 小样本不是「数字偏高」或「偏低」,是**数字没有意义**——
它同时高估了三个类别、低估了第四个。

**面试上的直接后果**:「拿二十条数据跑出来的指标会被当场戳穿」不是吓唬人。
28 条时我可以说「semantic 类 100%」,这句话确实是测出来的,
**但它经不起把评测集扩大一倍**。追问一层就穿帮的正是这种数字。

**证据强度**:受控实验。语料、索引、检索代码、指标定义全部没动,唯一变量是查询集。

### 45. 一个改动的好坏,取决于评测集里有没有它作用的那类查询

**现象**:扩到 60 条后新失败的 `value_lookup` 查询有个共同点——ground truth 所在的 chunk
**极小,但缺少词面桥梁**:

| 查询(英文) | ground truth | chunk 大小 | 结果 |
|--|--|--:|--|
| which embedding model is used | `MODEL_NAME = "all-MiniLM-L6-v2"` | **31 字符** | MISS |
| where are cloned repositories stored | `REPOS_DIR = ` | 50 字符 | MISS |
| execute_grep function(对照) | `def execute_grep(` | 2078 字符 | **命中** |

**块大小不是原因**——31 字符的块失败,2078 字符的成功。真正原因是那个小块里有
`MODEL_NAME` 和 `all-MiniLM-L6-v2`,**却没有 "embedding" 这个词**。
而它所在的文件叫 `indexer/embedder.py`——**桥梁在路径里,路径不在块里。**

再看原来 28 条里的 `value_lookup`:全是 `BLOCKED_COMMANDS` / `SHELL_OPERATORS` /
`MAX_STEPS` / `CHUNK_SIZE` 这类**常量名本身就和查询词重合**的,根本不需要桥梁。

**规则**:**我在 28 条上否决路径前缀(第 42 条)的依据,可能是不成立的**——
那个评测集里几乎没有「需要路径做桥梁」的查询。**同一个改动,换个评测集可能得出相反结论。**

这不是说第 42 条测错了,而是说**它的作用范围比我当时写的窄**:
结论只对「28 条那个查询分布」成立,不对「所有 `value_lookup` 查询」成立。
和第 38 条同一个形状,只是这次超范围的不是数据,是**评测集本身的代表性**。

> **评测集不只是量具,它同时定义了「什么算重要」。** 你的评测集里没有的查询类型,
> 对应的改进你永远看不见,对应的退步你也永远发现不了。

**证据强度**:诊断性观察(chunk 大小 + 词面桥梁缺失的对照)。
**⚠️ 这条的怀疑后来被实验否掉了一半,见第 48 条** —— 桥梁机制是真的,
但「所以换个评测集结论会翻」没有发生。诊断对了,推论错了。

---

## 十四、开源协作

### 46. 挑仓库投 PR 时,star 数和领域对口都是噪声

**现象**:为了给阶段 00B 找一个能真实提交 PR 的仓库,筛了 6 个候选。
先按直觉的两个条件排(star 100–1000、领域对口),再对每个仓库做一次**同口径测量**:
拉最近 100 个 closed PR,取 `merged_at` 非空的,**剔掉 contributors 榜前 5 名和 bot**,
剩下的就是「陌生人的 PR 被合了」的证据。

| 仓库 | star | 领域对口 | 最后 push | 外部 PR / 采样 | 判定 |
|--|--:|--|--|--:|--|
| `isaacus-dev/semchunk` | 660 | 切块库,**完美对口** | 2026-06-13 | **0 / 5** | ❌ |
| `zetaalphavector/RAGElo` | 129 | RAG 评测,**完美对口** | 2026-08-12 | 3 / 52 | ❌ |
| `Quansight/ragna` | 205 | RAG 编排 | **2025-07-12** | 49 / 86 | ❌ |
| `VectorInstitute/fed-rag` | 150 | 联邦 RAG | 2026-06-22 | 4 / 85 | ❌ |
| `agentevals-dev/agentevals` | 154 | agent 评测 | 2026-08-13 | 13 / 88 | ✅ |
| `dapr/dapr-agents` | 739 | agent 框架 | 2026-08-14 | 22 / 76 | ✅ |

**两个反直觉的方向都出现了**:

- `semchunk` 是最坑的一个。660 star、切块库、和我做过的 chunking 实验完美对口——
  但它**两年只有 5 个 merged PR,外部 0 个**。维护者直接往 main 推,
  **PR 流程根本不是这个项目的协作方式。** 只看 star 和领域会直接掉进去。
- `ragna` 反过来:外部 PR 占比 57%(49/86)是六个里最高的,
  但那是 2025 年 7 月之前的历史,**仓库已经停更 13 个月**。

**规则**:「这个仓库会不会合我的 PR」和 star 数无关、和领域对口无关,
只和一件事有关:**它历史上合过多少非核心成员的 PR,且现在还活着。**
这两个条件必须**同时**成立——单看任一个都会选错,而且是往相反方向错。

### 47. 用行为记录判断,不要用自称的身份字段

**现象**:第一轮我用 GitHub API 的 `author_association` 字段区分内外部贡献者
(`MEMBER`/`OWNER`/`COLLABORATOR` 算内部)。RAGElo 的结果是 **52 / 52 全是外部**,
好得不真实。换成「作者是否在 contributors 榜前 5」的口径重测,变成 **3 / 52**。

**同一个仓库,同一批 PR,两个口径差了 17 倍。**

原因:**非公开的 org 成员在 API 里也返回 `CONTRIBUTOR`**。
RAGElo 的核心维护者 `ArthurCamara`、`din0s` 全被这个字段算成了「外部贡献者」。

**规则**:判断一个社区开不开放,要用**行为记录**(谁的代码实际进了主干、进了多少次),
不要用**自称的身份字段**。身份字段反映的是「有没有公开声明这层关系」,不是事实本身。

和第 31 条(ground truth 用字符串不用行号)是同一个形状:
**在两个信号之间选,永远选那个不依赖别人如实填写的。**

**顺带一个陷阱**:`good first issue` 标签 ≠ 可以做。
`dapr/dapr-agents` 挂着 3 个 GFI,**全部已经有 assignee**(#472、#375),
GitHub 的搜索过滤器不看这个。**必须逐个查 `assignees` 和评论区**——
有人在评论里说「I'd like to work on this」但没被 assign 的,也已经算被占了。

**证据强度**:受控测量,六个仓库同一套口径。
**已知局限**:GitHub API 单页上限 100 条,所以各仓库的**采样时间窗不同**——
活跃仓库的 100 个 PR 可能只覆盖 3 个月,冷清仓库可能覆盖 3 年。
所以上表的绝对数字不能横向比,**能比的是「有没有」和「最近一个是什么时候」**。

---

## 十五、验证纪律

### 48. 局部按机制生效,整体仍然是负的

**现象**:第 45 条怀疑「路径前缀是被一个不具代表性的评测集冤枉的」,
于是用 60 条重跑了**完全相同**的改动(同一个补丁,chunk 数同样是 213)。

我预先写下一条可证伪的断言:三条桥梁词齐全的英文 `value_lookup` 查询
(`embedding`→`indexer/embedder.py`、`repositories`→`tools/repo.py`、
`vector`→`store/vector.py`)**至少有两条应该从 MISS 变成命中**。

结果:

| id | 加前缀前 | 加前缀后 | |
|--|--|--|--|
| q34 which **embedding** model is used | MISS | **rank 4** | 救回 |
| q37 where are cloned **repositories** stored | MISS | rank 10 | @10 算,@5 不算 |
| q40 how many results does **vector search** return | MISS | MISS | 没救回 |

**按 @5 口径 1/3,断言不成立**(按 @10 是 2/3)。

**但机制本身被证实了。** q34 的 top-1 和 top-2 **都来自 `indexer/embedder.py`** ——
路径桥梁确实把对的文件捞了上来,只是正确的那一块排在同文件另外两块的后面。

整体结果(英文 @5,n=60):

| 类别 | 基线 | 加前缀 | Δ |
|--|--:|--:|--:|
| `exact_identifier` | 100% | 87% | **-13** |
| `semantic` | 81% | 75% | -6 |
| `value_lookup` | 79% | **71%** | **-8** |
| `cross_file` | 46% | 43% | -3 |
| **整体** | **76%** | **69%** | **-7** |

**`value_lookup` 整体是降的,尽管我点名的三条里有一条被救回来了。**
中文侧仍然只有 `value_lookup` 受益(36% → 50%),整体 38% → 41%。

**规则**:**「这个改动在我指定的几条查询上按机制生效」和「这个改动值得做」是两件事。**
挑几条「它应该能救」的查询去验证机制,是在**验证因果**;
评估值不值得做,要看**它在全部查询上的净效果**。前者成立完全不蕴含后者。

这次桥梁增益是真的(q34 从 MISS 到 rank 4),但它换来的是每一块都变钝一点,
**失去的比捞回来的多**。局部有效 ≠ 整体有效。

**一个被自己否掉的假设**:我还猜前缀会把检索粒度从「块」拉向「文件」
(找对文件、选错块)。量了一下 top-5 里的平均不同文件数:
**基线 3.267 → 加前缀 3.150**,只差 0.12。方向对,但**幅度太小,不足以当解释**。
q34 那个「同文件两块霸榜」的例子是真的,但它是个案,不是系统性效应。
**一个漂亮的个案不能升级成机制** —— 这是第 38 条的又一次复发。

**最终结论**:第 42 条的否决**维持**,而且比当初更硬(n=60 上 -7,n=28 上 -9)。
第 45 条的怀疑是**合理但错误**的:评测集确实不具代表性,
但补上代表性之后结论没有翻,只是理由变清楚了。

> 怀疑自己的结论、设计一个能推翻它的实验、然后接受「原结论其实是对的」——
> 这个过程没有白费。**没做这次重跑之前,第 42 条是「碰巧对」;做完之后它才是「可信地对」。**

**证据强度**:受控实验。同一补丁、同一语料、同一索引流程(chunk 数 213 未变)、
同一指标,唯一变量是评测集规模。改动已回滚,基线八个数字逐格恢复。

### 49. 融合两个检索器之前,先单独测每一个

**现象**:做混合检索(向量 + BM25,RRF 融合),直接实现了融合就跑,结果英文整体
**76% → 64%**,`value_lookup` 一个类别掉了 **36 个百分点**。花了一整轮排查,
才回头做了本该最先做的那件事——**把两个臂分开测**:

| 类别(英文 @5) | 向量单独 | BM25 单独 |
|--|--:|--:|
| `exact_identifier` | 100% | 93% |
| `semantic` | 81% | 56% |
| `value_lookup` | 79% | **36%** |
| `cross_file` | 46% | 27% |

**BM25 在每一个类别上都输给向量**,而且唯一接近的 `exact_identifier`
恰好是向量已经 100% 的那一类。

**规则**:**如果一个臂在所有类别上都被另一个臂支配,融合不可能有增益,只能有稀释。**
融合的前提是**互补**——A 在某类上赢 B,B 在另一类上赢 A。没有互补就没有可融合的东西。

这个检查五分钟就能做(两个检索器各跑一遍评测),**应该排在实现融合之前**。
我把顺序做反了,代价是一整轮排查。

> 先量「有没有互补」,再谈「怎么融合」。**没有互补的时候,调权重是在给噪声调音量。**

**证据强度**:受控实验。同一评测集、同一索引、同一指标,唯一变量是检索器。

### 50. RRF 奖励一致性,候选深度相对语料太大就会反噬

**现象**:第一版融合用 `CANDIDATE_K=50`,而语料只有 **213 个块**——每个候选列表覆盖了
23% 的语料。逐条诊断发现向量本来排第一的被挤到了后面:

| 查询 | 向量 rank | BM25 rank | 融合后 |
|--|--:|--:|--:|
| what is the maximum number of agent loop steps | **1** | 44 | **12** |
| what is the default model identifier | **2** | 无 | **22** |
| how many lines per chunk and how much overlap | **2** | 21 | **9** |

**规则**:RRF 的分数是 `Σ 1/(k+rank)`,**同时出现在两个列表里的文档白拿一份加分**。
候选列表越深,平庸文档「两边都沾边」的概率越高,**只被一个检索器找到的文档会被系统性压制,
不管那个检索器多确信**。

教科书上的 RRF 是在百万级语料里取 top-10,两个列表几乎不重叠;
**在 213 个块里取 top-50,重叠是常态,机制就反过来了。**
`CANDIDATE_K` 不是无关紧要的实现细节,它要**相对语料规模**来设。

实测:把 `w_bm25` 从 1.0 降到 0.3,`value_lookup` 从 43% 回到 64%——
**参数确实是那 36 个点里的一大半**。但九组参数扫下来没有一组打得过纯向量:

| cand_k | w_bm25 | 整体(英文 @5) |
|--:|--:|--:|
| — | 纯向量 | **76%** |
| 10 | 0.3 / 0.5 / 1.0 | 67% / 67% / 64% |
| 20 | 0.3 / 0.5 / 1.0 | 66% / 67% / 66% |
| 50 | 0.3 / 0.5 / 1.0 | 68% / 68% / 64% |

**参数错了会让结果更差,但参数对了也救不回一个没有互补性的方案。**
先分清「调参问题」和「方向问题」,不然会在错的方向上一直调下去。

**证据强度**:受控参数扫描,九组 + 基线,同一评测集同一索引。

### 51. 代码语料上,BM25 跨不过缩写这道词法鸿沟

**现象**:BM25 在 `value_lookup` 上只有 36%。看查询词和目标代码块的**词集交集**:

| 英文查询 | 目标代码 | 交集 |
|--|--|--|
| what is the **maximum** number of agent loop **steps** | `MAX_STEPS = 10` | `{steps}` |
| where are cloned **repositories** stored | `REPOS_DIR = Path` | **空集** |
| how many **results** does **vector search** return | `n_results: int = 5` | `{results}` |

**规则**:**自然语言用完整词,代码用缩写**——`maximum`/`MAX`、`repositories`/`REPOS`、
`configuration`/`CONFIG`。BM25 没有词形归一化就跨不过去,
**而 embedding 恰好能,因为它训练时就学过 max ≈ maximum**。

所以在「英文自然语言查询 + 代码语料」这个组合上,**向量做的正是 BM25 做不到的事**,
而 BM25 唯一的强项(罕见字面 token)是向量**已经做到 100%** 的那一类。没有互补空间。

**这不是说混合检索没用**,而是说它的收益取决于**查询和语料的词形是否对齐**:
- 查询里直接写标识符(`BLOCKED_COMMANDS 的值是什么`)→ BM25 rank 1,和向量并列
- 查询是自然语言描述 → BM25 基本失效

**推论**:要在代码上用 BM25,先补词形处理(stemming、缩写词典、标识符展开),
**否则测到的是「没做词形归一化的 BM25」,不是「BM25」**。这个前提没做,
结论的作用范围就只到这里为止。

**我错在哪**:预测里我断言 q50(四个期望块都含字面 token `TOOLS`)
BM25 至少能捞回 2/4。实际 **0/4**——BM25 的 top-5 全被 `README.md` 占了,
因为 README 里 "tool" 出现次数远多于代码块,**TF 高、IDF 又被拉低**。
我在预测里其实写了这个风险("`TOOLS` 这种词到处都是,IDF 会被拉低"),
**但还是把断言定在了「至少 2/4」而不是「可能 0/4」**——
写下了风险却没让它影响结论,等于没写。

**证据强度**:受控实验 + 词集交集的直接观察。

---

> **以下 52–54 来自读文章,不是我自己的实验。**
> 来源:Prem AI 博客《Building Production RAG Architecture》(2026),全文中文译稿见
> `agentevals/生产级RAG架构-中文翻译.md`。
> **证据强度整体偏弱:二手数字,没有复现。** 记下来是因为它们给出了可检验的假设,
> 不是因为它们已经被验证。凡是和我自己实测冲突的地方,以实测为准(见 54)。

### 52. 分块不是预处理,是检索质量的主变量

**现象**:文章引 2025 年一项临床决策支持研究——**同一语料**下,自适应分块准确率 87%,
固定长度基线 13%。同一份文档、同一个嵌入模型、同一个检索器,只换分块方式,差 6 倍多。

**规则**:**分块策略由「文档类型 × 查询模式」共同决定,不存在全局最优的 chunk size。**
所以「512 token + 10% overlap」不是一个默认值,是一个**只对同质化文本成立的特解**。
调 RAG 的顺序应该是**先摄入侧、再检索侧、最后才是 prompt**,
因为越靠前的环节丢掉的信息,后面所有环节都补不回来。

**这条要怎么验**:拿我自己的语料,固定其他一切,只扫 chunk 策略(固定 / 递归 / 层级),
看差距是不是也在数量级上。**没验之前 87% vs 13% 只是别人语料上的一个点。**

**证据强度**:二手,单一研究,未复现。

### 53. 优化前先量各环节耗时,否则会在非瓶颈上使劲

**现象**:文章给的典型延迟分布——查询嵌入 20–50ms,向量检索 5–30ms,BM25 5–20ms,
rerank 30–100ms,**LLM 生成 500ms–3s+**。整个检索链路加起来通常不到 200ms,
生成端一个人就吃掉 80%–95%。

**规则**:**瓶颈不在你最熟悉的那一环,而在你没测过的那一环。**
在检索侧省 15ms(比如 BM25 和向量并行跑)对端到端毫无感知,
但同样的工程时间换成流式输出,用户感知延迟直接从「3 秒空白」变成「200ms 出字」。

更一般的形式:**优化的收益上限 = 该环节占总耗时的比例**(Amdahl)。
所以 profile 是优化的前置条件,不是可选步骤。这条和 rerank 的取舍也对上了——
rerank 加 50–100ms 换精度 +10–30%,**因为它加在了本来就便宜的那一侧**,所以划算。

**证据强度**:二手数字,但和 Amdahl 定律一致,机制上站得住。

### 54. 混合检索的收益取决于词形是否对齐——这条我实测过,和文章说的不一样

**现象**:文章的主张是「纯 dense 检索会漏掉精确匹配类查询(编号、代码、法条),
所以必须上 BM25 混合」。**但我在 49/50/51 里实测的结论更窄**:
在「英文自然语言查询 + 代码语料」上,BM25 只有 36%,加进来非但没帮上忙,
RRF 融合后还比纯向量更差。原因在 51:自然语言用完整词、代码用缩写
(`maximum`/`MAX`、`repositories`/`REPOS`),BM25 没有词形归一化就跨不过去,
**而这正是 embedding 天然能做的事**。

**规则**:**「要不要混合检索」不取决于语料里有没有标识符,取决于查询里会不会出现标识符的字面形式。**
- 用户直接打 `BLOCKED_COMMANDS 的值是什么` → 字面 token 对齐,BM25 rank 1,混合有收益
- 用户问 "where are cloned repositories stored" → 词集交集为空,BM25 贡献负值

所以文章那条建议**默认了一个前提:查询和语料的词形是对齐的**。
在检索用户手打的产品编码、RFC 编号时这个前提成立;在代码检索上通常不成立。

**更值得记的是这个模式本身**:一条工程建议被广泛传播时,**前提条件往往被省略了**,
而省略掉的前提正是它失效的地方。读到「必须做 X」的时候,
要还原的问题是「**X 在什么条件下才有收益**」,而不是「X 对不对」。

**证据强度**:文章那半是二手;**我这半是受控实验(49–51),同一评测集同一索引,可复现。**
冲突时以后者为准,但注意后者的作用范围也只到「没做词形归一化的 BM25 + 代码语料」为止。

### 55. 先分清「召回问题」和「排序问题」,再决定上不上 reranker

**现象**:`cross_file` 一直是最弱的一类(英文 coverage@5 = 46%)。
上 reranker 之前先量了一件事:那些没进 top-5 的期望块,
到底是**根本没被召回**,还是**召回了但排在后面**?

把 36 个期望块在全量 213 名里的真实排名列出来:

| 排名区间 | 英文 | 中文 |
|--|--:|--:|
| 1–5(已在 top-5) | 42% | 6% |
| 6–50 | **47%** | 14% |
| 51–100 | 11% | 28% |
| 101–213 | **0%** | **50%** |

完美 reranker 的天花板(假设能把候选池里的正确块全提到前 5):

| 候选池深度 N | 英文 | 中文 |
|--:|--:|--:|
| 5(现状) | 46% | 4% |
| 20 | 74% | 11% |
| **50** | **93%** | 20% |
| 100 | 100% | 48% |

**规则**:**reranker 只能重排已经召回的东西——它修的是排序问题,不是召回问题。**
所以上它之前必须先量一件事:目标在候选池里吗?

- **在** → 排序问题 → reranker 有效,**天花板 = 候选池的 coverage**
- **不在** → 召回问题 → reranker 一点用都没有,要改的是第一阶段
  (embedding 模型 / 查询改写 / 索引策略)

这次最值钱的地方是**同一个系统给出了两个相反的答案**:
**英文是排序问题**(89% 的期望块在 top-50 内,+47pp 空间),
**中文是召回问题**(一半排在 100 名开外,N=50 天花板才 20%)。
**同一个 reranker,对英文查询价值巨大,对中文查询几乎没用。**

逐条看更清楚:15 条 `cross_file` 里 **13 条的全部期望块都在 top-50 内**,
离群点只有 q50(`[84, 40, 52, 60]`)和 q28(`[17, 36, 77]`)。
而 q51 `[6, 7]`、q56 `[6, 12]`、q57 `[12, 20]` 这种**差一点点就进 top-5**,
正是 reranker 最容易拿分的形状。

**顺带量到的**:`semantic`(81%)和 `value_lookup`(79%)空间同样大,
而且 **N=20 就能到 100%**——它们不需要像 `cross_file` 那样把候选池开到 50。
**不同类别的最优候选深度不一样**,一刀切设一个 N 会浪费重排预算。

> **这个测量花了 20 分钟、没改一行代码,却直接决定了下一步做什么、以及做了之后的上限在哪。**

和第 36 条(优化某一层前先确认瓶颈在那一层)、第 53 条(优化前先量各环节耗时)是同一个家族,
但这条补的是**再下一层**:

**确认瓶颈在这一层之后,还要确认你打算用的那个工具,能治的是不是这一层的病。**
reranker 治排序、不治召回;第 53 条那个延迟分布同理——
rerank 只要 30–100ms,而 LLM 生成 500ms–3s,**在检索链路上抠延迟是在非瓶颈上使劲**。

**证据强度**:直接测量。对 60 条查询取全量 213 名排名,不涉及任何改动,无预测无归因风险。

### 56. 通用 reranker 对短块有系统性偏见

**现象**:cross-encoder 重排(`ms-marco-MiniLM-L-6-v2`),候选池 N=50,英文 @5:

| 类别 | 基线 | 重排后 | Δ |
|--|--:|--:|--:|
| `exact_identifier` | 100% | 100% | ±0 |
| `semantic` | 81% | **88%** | +7 |
| `value_lookup` | 79% | **71%** | **-8** |
| `cross_file` | 46% | 47% | **+1** |
| **整体** | 76% | 77% | +1 |

**天花板是 93%(上一条量过),实际只吃到 +1——可用空间的 2%。**

先猜是「重排破坏了结果多样性」,量了一下 **被证伪**:top-5 平均不同文件数
基线 3.27 → 重排 3.33,几乎没变。

真正的模式在丢失的条目里:`q35`(66 字符)、`q36`(75 字符)从命中变未命中,
而 `q40`(3350 字符)反过来从未命中变命中。聚合验证:

```
top-5 里 <200 字符的小块:   基线 36 个  →  重排后 14 个   (丢掉 61%)
top-5 chunk 字符数中位数:   657        →  1092          (+66%)
```

**规则**:**通用 reranker 的训练分布是自然语言段落(MS MARCO 是网页 passage),
一个 66 字符的 `DEFAULT_MEMORY_PATH = Path(...)` 长得完全不像它见过的东西,
于是被系统性打低分。** 模型没见过的形态,它给的不是「不确定」,是「不相关」。

**用在代码检索上之前,先问它是在什么分布上训练的**——
`exact_identifier`(块中等)零影响、`semantic`(块中等偏大)+7、
`value_lookup`(全是极短常量块)-8,**受害程度和块长度完全对应**。

### 57. 候选池不是越深越好

**现象**:同一个 reranker,只改候选池深度(英文 @5):

| 配置 | exact | semantic | value | cross | 整体 |
|--|--:|--:|--:|--:|--:|
| 基线(不重排) | 100% | 81% | 79% | 46% | 76% |
| 重排 N=10 | 100% | 81% | 71% | **50%** | 76% |
| **重排 N=20** | 100% | **88%** | **79%** | 47% | **78%** |
| 重排 N=30 | 100% | 88% | 71% | 47% | 77% |
| 重排 N=50 | 100% | 88% | 71% | 47% | 77% |

**N=20 最好,比 N=50 还好。**

**规则**:候选池深度有**两个方向相反的效应**——
池子越深,**天花板越高**(正确块更可能在里面);
但池子越深,**reranker 犯错的机会也越多**(更多干扰项可以被误提到前面)。

**净效果取决于 reranker 的准确率**。它足够准时,深池子才划算;
它在你的领域不准(比如上一条那个短块偏见),深池子只是给它更多机会犯错。

「天花板测量」告诉你**上限在哪**,但**不告诉你能吃到多少**——
N=50 天花板 93%、实际 47%,利用率 2%。
**上限高不等于值得把池子开到那么深。**

### 58. 同一个改动,在不同 k 上的价值完全不同

**现象**:reranker 在 @5 上只 +1,但看别的 k:

| | @1 | @3 | @5 | @10 |
|--|--:|--:|--:|--:|
| 基线(英文) | 36% | 59% | 76% | 81% |
| 重排后 | **52%** | **70%** | 77% | 84% |
| Δ | **+16** | **+11** | +1 | +3 |

**同一次改动,@1 涨 16 分,@5 涨 1 分。**

**规则**:reranker 擅长的是**把最好的那一个顶到最前面**,不是**填满 k 个格子**。
所以它的价值**完全取决于你的产品用哪个 k**:

- 只展示一条答案 / 喂给 LLM 一个片段 → 看 @1,**+16 分,非常值**
- 要凑齐多块拼一个答案(`cross_file` 那种)→ 看 coverage@5,**+1 分,不值**

**「这个改动有没有用」是个没有意义的问题,除非先说清楚用哪个指标。**
我差点只看 @5 就把 reranker 判死刑。

### 59. 「要不要 RAG」是成本问题,不是能力问题——先量语料多大

**现象**:量了自己这个语料到底多大:

```
可索引文件      37 个
切块后          213 个 chunk, 209,920 字符
token 数        约 52,480      ← 完全塞得进 200k 窗口
检索 top-5      约  1,231      ← 只送 2.3% 的语料
```

**52k tokens 意味着「不用 RAG,全塞进去」在技术上是可行的。** 所以取舍不是「能不能」,是「值不值」。

单轮成本(Sonnet 输入 $3/M):全塞 $0.157,检索 $0.004,**43 倍**。

**但单轮差价会骗人。** API 无状态、历史每轮全部重发(第 17 条),成本是 **O(N²)**:

| 10 轮对话 | 累计 tokens | 成本 |
|--|--:|--:|
| 全塞 | 2,886,400 | **$8.66** |
| 检索 | 67,705 | **$0.20** |

**规则**:讲长上下文成本,不要说「长上下文贵」——要说
**「长上下文的成本随轮数平方增长,而 RAG 把那个平方项的常数压小了 43 倍」**。
前者是印象,后者是能被追问的工程判断。

**决定用哪个的四个维度**:

| 维度 | 长上下文赢 | RAG 赢 |
|--|--|--|
| 语料规模 | 稳定且 < 窗口 | 超窗口或持续增长 |
| 调用频率 | 单次、低频 | 高频 / 多轮 |
| 更新频率 | **无索引可过期**,永远最新 | 改了要重建索引 |
| 答案分布 | 需要全局理解 | 答案集中在少数几处 |

第三行是最容易漏的:我们每改一次 `chunker.py` 就必须跑 `reindex.py`,
**索引是一份会过期的副本**——长上下文没有这个负担。

### 60. 我自己的评测集同时支持两边,所以答案是路由不是二选一

**现象**:同一份评测数据,两类查询指向相反的结论。

**支持长上下文**:`cross_file` 英文 coverage@5 只有 **46%**。
这类查询问的是「完整流程涉及哪些模块」「有几种 agent 实现」——
**需要全局视野,而 RAG 天生只给局部**。全塞进去这类问题直接消失。

**支持 RAG**:`exact_identifier` 已经 **100%**。
花 52k tokens 回答「`execute_grep` 在哪定义」是纯浪费。

**规则**:**长上下文 vs RAG 不是二选一,是按查询类型路由**——
精确查找走检索,全局理解走长上下文。

**但这句话的前提是你能分辨查询类型。** 没有分类评测集,你看到的只是一个
76% 的总分,**看不出哪一类该走哪条路**,只能整体二选一。

> **这条是四个杠杆的收束点:分类评测集不只是用来测分数的,
> 它是做架构路由决策的依据。**
> 面试里「你怎么知道你的检索是好的」下一个追问往往是
> 「那你为什么选 RAG 不选长上下文」——这两问的答案是同一份资产。

**证据强度**:语料规模是实测(chars/4 估算 token,代码更密所以真实值偏高,
方向不受影响);成本是按公开价目表算的;两类查询的分数是受控实验测的。

---

### 61. 快照测试必须断形状,不能只断内容

**现象**:harness 的 `FakeProvider` 每次被调用时记一份 messages 快照,写的是
`self.calls.append(list(messages))`。把 `list()` 去掉(变成存引用),13 条测试**只挂 1 条**。

挂的那条断言的是形状:

```python
assert len(second_call) == 3
```

其余 6 条读 `calls` 的测试**全部假绿**,因为它们断言的是内容:

```python
result_block = provider.calls[1][2]["content"][0]   # 还能取到,而且值全对
```

`loop.py` 里 `messages` 是原地 append 的同一个对象,去掉拷贝后
`calls[0] is calls[1]` 为 `True`,两个都指向长度 4 的最终列表。
第 2 项**碰巧**就是那条装 `tool_result` 的 user 消息,所以取值全中、断言全过——
但它们已经不在测「第 2 次请求发出去的是什么」,而是在测「跑完之后列表里有什么」。
**测的对象悄悄换了,信号没了,颜色还是绿的。**

**规则**:对可变对象记快照,别名 bug 只有**形状断言**能抓到。
内容断言会在别名下继续通过,**而且看起来更像在认真测**。
凡是快照类的测试,至少要有一条断长度或结构。(证据强度:实测,13 条里 12 条绿)

附带一条:`list(x)` 是**拷贝**不是类型转换,两边类型都是 `list`。
按「类型不符」去理解这个 bug,会去加类型标注或 `cast()`,bug 原样留着。
同族的坑:可变默认参数、`copy` vs `deepcopy`、切片是浅拷贝——
**都是「看起来在存值,实际存的是引用」。**

### 62. 解耦依赖 ≠ 解耦运行时版本

**现象**:`def check_command(command: str) -> str | None:` 这一行在 `my_agent.py` 里一直正常,
因为它跑在 `05-agent-loop` 的 Python **3.14** venv 里。把同样的代码搬进不依赖任何 venv 的
`harness/` 之后,用系统 python3(**3.9.6**)一跑就崩:

```
TypeError: unsupported operand type(s) for |: 'type' and 'NoneType'
  File "loop.py", line 37, in <module>
```

注意是**导入期**崩,不是调用期——`X | None` 这种写法要 3.10+。

**规则**:把代码从「依赖某个 venv」改成「不依赖」的那一刻,
也把它暴露给了机器上更低的语言版本。
**解耦依赖和解耦运行时版本是两件事:前者是主动做的,后者是附带发生的,而且不会有人提醒你。**
(证据强度:实测)

修法是 `from __future__ import annotations`。
接第 39 条——「目录独立」不等于「能独立运行」,这条是它的运行时版本。

**反过来也是收益**:正因为解耦干净了,那 13 条测试现在**不需要 venv、不需要装 anthropic、
不需要 API key**,系统 python3 直接跑。代价是要守住两条纪律:
不用新语法,SDK 的 import 留在构造函数里(提到文件顶部就白做了)。

### 63. grep 的搜索路径就是你的采样范围

**现象**:要判断 `tau` 这个仓库有没有做崩溃恢复,我 grep 了它的核心包 `src/tau_agent/`,
得到一张全零的表,于是写下结论「tau 没有 durable execution,这一层只有 TypeScript 参考」。

把搜索路径放大到整个 `src/` 之后:

```bash
for w in resume record durable crash idempot; do
  printf "%-10s tau_agent/:%s  全 src/:%s\n" "$w" \
    "$(grep -ril "$w" src/tau_agent | wc -l)" "$(grep -ril "$w" src | wc -l)"
done
```

```
resume     tau_agent/:0   全 src/:12
record     tau_agent/:0   全 src/:16
durable    tau_agent/:2   全 src/:14
crash      tau_agent/:0   全 src/:8
idempot    tau_agent/:0   全 src/:2
```

东西一直在,只是在**另一个包**里(`src/tau_coding/`)。基于那个错结论,我给整整一层的
学习计划指错了材料——「去读 TypeScript 设计文档」,而实际有 300 多行现成的 Python 可读。

**规则**:**grep 的路径参数就是采样范围,而结论的范围往往被默认写成整个仓库。**
「核心包里没有」和「这个仓库没有」是两个命题,前者不蕴含后者——
分层架构里,基础设施性质的东西天然沉在**上层包**或**外围包**,不在被命名为 "core" 的那个。
用 grep 判断「有没有某个能力」时,**先跑一次不带路径限制的,再往下收窄**。
(证据强度:实测,同一组词两个范围差 8–16 倍)

这是第 38 条的一个具体形态:**采样范围小于结论范围。**
讽刺的是我同一天刚拿第 38 条修正了第 8/10 条(只测过 Anthropic 一家却写成协议层普遍规律),
转头自己犯了一遍。**这类错误不会因为你刚讲过就免疫。**

### 64. 检验理解的问题必须有判据,否则答错不会现形

**现象**:agent 写完 `provider.py` 后出了三道追问题验我是否真懂:

| 题 | 有没有判据 | 结果 |
|--|--|--|
| 1. `client` 参数是不是多余的? | ❌ 开放 | 我答「删掉」,agent 说「对」 |
| 2. 为什么是 `append(list(messages))`? | ✅ **跑一次测试就能判死** | 我答「类型不符合」,**错**,当场现形 |
| 3. 透传 SDK 对象的代价? | ❌ 开放 | 我答「换 DeepSeek 时要付」,agent 说「对」 |

**三道里只有第 2 道产出了东西**(变成第 61 条)。第 1、3 题我答什么都成立,
agent 只能顺着说「对」——**它们没有能让我答错的机制。**

**规则**:验证理解的问题,**必须存在一个能宣告「你错了」的判据**——
一个能跑的命令、一个具体数字、一条会挂的断言。没有判据的问题不是在检验理解,
是在**征求同意**,而人对自己似懂非懂的地方总会给出听起来合理的答案。

可用形式:**预测题**——先写下具体数字或具体报错,再跑。
「把 headroom 从 4096 改成 512,几条测试会挂?」有判据;
「为什么要留 headroom?」没有。
(证据强度:实测,1/3 命中)

**对面试的直接含义**:面试官追问第二层时用的就是判据式提问
(「那 OpenAI 的 tool 角色算什么」「快照多大」「打快照时正在跑的工具怎么办」)。
**平时只做开放题自测,等于只对着不会反驳的人排练。**


### 65. 评测指标的匹配函数必须和 ground truth 的形状配套

**现象**:把 Ragas 的 `non_llm_context_recall` 接到自己的 60 条检索评测集上,
同一个索引、同一个检索器、同一批查询,**只换指标**:

```
                     我的 hit/coverage@5    Ragas non_llm_context_recall
整体                        76%                      7%
  exact_identifier         100%                      0%     ← 完全相反
  semantic                  81%                      0%
  value_lookup              79%                     29%
  cross_file                46%                      0%
```

60 条里 42 条判定不一致。最大相似度中位数 **0.058**,而阈值是 **0.5**,只有 4/60 越过。

**原因不是「分数偏低」,是数学上够不到。** Ragas 的判定是
`1 - 归一化Levenshtein(chunk, needle) > 0.5`,归一化按 `max(len)` 除,
而编辑距离至少是两串长度差,于是存在硬上限:

```
相似度 ≤ len(needle) / len(chunk)

  chunk   67 字符   上限 0.269
  chunk  215 字符   上限 0.084
  chunk  781 字符   上限 0.023   ← 我的 chunk 中位数
  chunk 6118 字符   上限 0.003
```

我的 ground truth 是 `"def check_command("` 这种代码子串(中位 **18** 字符),
chunk 中位 **781** 字符 → 上限 0.023。`exact_identifier` 类恒等于 0,
**分数与检索质量完全无关**。`value_lookup` 能拿到 29%,唯一原因是那几条的 chunk 特别短。

**规则**:**换指标前,先算这把尺子的取值范围能不能覆盖你要区分的差异。**
指标由两部分组成——匹配函数 + 阈值/聚合,而匹配函数对输入形状有隐含假设。
长度敏感的相似度(编辑距离、Jaccard)假设两侧长度可比;
拿它匹配「短标识符 vs 长文档」,输出的是长度比,不是相关性。
子串包含则相反,它对长度免疫但对措辞零容忍。
**ground truth 的形状(短标识符 / 整段文本 / 结构化字段)决定了哪一族匹配函数可用。**
(证据强度:实测 60 条 + 公式推导 + 五组长度实测)

### 66. 生成侧指标测不出检索失败

**现象**:用自己的 `score@5` 把评测集切成两组——检索全对(HIT)和检索一条正确 chunk 都没捞到(MISS),
各 13 条,跑 Ragas 的 LLM 判官指标(DeepSeek 当判官):

```
指标                  HIT 组    MISS 组     差距
faithfulness           0.82      0.62      0.21
answer_relevancy       0.66      0.18      0.48

对照: 我的 score@5      1.00      0.00      1.00
```

**MISS 组 13 条里,7 条 faithfulness = 1.00(满分)。** HIT 组也只有 10/13 满分,
**两组分布大面积重叠**——0.21 不是弱信号,是噪音。

最典型的两条(q57 / q59):检索一条正确 chunk 都没命中,
但 faithfulness = 1.00、answer_relevancy = 0.99 / 0.92,答案内容读起来还基本正确。
**三个指标全绿,检索是坏的。**

**规则**:**faithfulness 问的是「答案有没有依据」,不是「依据对不对」。**
检索错了、模型忠实复述错内容,它照样满分——这是定义决定的,不是实现缺陷。
所以 **RAG 评估必须两条腿分开量:检索侧要有独立的、带 ground truth 的召回指标。**
只看端到端的生成侧分数,检索退化会被生成器的忠实掩盖,
而且掩盖得最彻底的恰恰是最危险的情况——答案自信、切题、有依据、但依据是错的。
(证据强度:实测 26 条,两组各 13)

### 67. 指标的区分度,不等于它测量了你以为的那件事

**现象**:上一条里 `answer_relevancy` 两组差 **0.48**,看起来能干净地区分检索好坏。
但逐条拆开:**MISS 组 13 条里有 10 条的分数恰好等于 0.00**——不是「相似度低」,是被清零。

读 `ragas/metrics/_answer_relevance.py:126` 的聚合公式:

```python
all_noncommittal = np.all([answer.noncommittal for answer in answers])
score = cosine_sim.mean() * int(not all_noncommittal)
```

**答案被判定为「回避」(noncommittal)就整体乘 0。**
我的生成提示词里写了 "If the context does not contain the answer, say so",
MISS 组拿不到正确上下文 → 老实说「没有」→ 判为回避 → 清零。
那 0.48 的差距几乎全部来自「模型肯不肯认输」,不是「检索对不对」。
换一个爱编的模型,MISS 组分数会立刻反弹,而检索一样烂。
**HIT 组也有 3 条被同样清零**,说明这个开关还会误伤。

**规则**:**用一个指标之前先读它的聚合公式,把连续量和开关量分开。**
连续量(余弦相似度)反映程度,开关量(`int(not all_noncommittal)`、阈值二值化、
`np.nan` 兜底)反映的是某个条件触没触发。
**当开关量主导时,分数差反映的是那个开关的触发条件,不是指标名字承诺的语义。**
判断方法很便宜:看分数分布里有没有大量「恰好等于 0 / 恰好等于 1」的堆积——
连续量不会精确堆在端点上。
(证据强度:实测 + 读源码确认机制)

### 68. 库在接口迁移期,自带的实现可能不满足它自己的旧接口

**现象**:两个坑连着踩,都出在 ragas 0.4.3 这种「新旧接口并存」的版本上。

1. `pip install ragas` 装完直接 `import ragas` 就崩:
   `ModuleNotFoundError: No module named 'langchain_community.chat_models.vertexai'`。
   ragas 0.4.3 里写的是**直接子模块导入**,而 langchain-community 0.4.x 把
   `chat_models/` 下的文件全删了改成懒加载(目录里只剩 `__init__.py`)。
   解法:`langchain-community<0.4`。另外 `rapidfuzz` 是非 LLM 指标的必需品但不是硬依赖,要单独装。

2. ragas 包里同时存在**两套 embedding 基类**:`BaseRagasEmbedding`(新,`embed_text`)
   和 `BaseRagasEmbeddings`(旧,`embed_query` / `embed_documents`)。
   它自带的 `HuggingFaceEmbeddings` 实现的是**新**的,
   而 `ResponseRelevancy` 调的是**旧**的 → `AttributeError: no attribute 'embed_query'`。
   **官方类喂给官方指标,直接不兼容。**

**规则**:**看到 `FooThing` 和 `FooThings` 这种单复数并存、或 import 时冒出
DeprecationWarning 说「v1.0 会移除,请改用 X」,就说明这个包正在接口迁移期。**
迁移期的包有两个特征:①对上游依赖的版本容忍度极窄(它按旧版 API 写死);
②内部新旧两套并存,而**新实现不保证满足旧接口**——官方类喂给官方函数会炸。
应对:钉死上游依赖版本;**不要假设「同一个包里的东西互相兼容」,
按报错里那个具体的方法名去找真正需要的接口**,必要时写十行适配器,
比再引一个 langchain 依赖便宜得多。
(证据强度:实测两次,均读源码确认了原因)

### 69. 机制存在 ≠ 机制被触发,中间隔着触发概率

**现象**:把 `rag-eval` 的向量库从 ChromaDB 换成 Qdrant——embedding 模型、chunk、
距离函数全不动,只换库。跑之前我预测「**会变**」,理由是:

同一次查询的 top-5 里,第 4、5 名的余弦相似度**完全相等**(都是 0.3723)。
并列时谁排前面由各库的内部 id / 插入顺序决定,**两个库的规则不一样**。
判分是 `first_hit_rank(...) <= k`,所以 tie 换序应该会影响 @k。

**实测:两个库的完整输出 `diff` 零行差异。** 60 条查询 × 4 个 k × 中英两套,
240 个数字、逐条排名,一个字节都没差。

推理链没错,漏的是**触发条件的合取**。tie 要真正翻转判分,需要**三件事同时成立**:

1. 存在并列分数
2. 并列的两个恰好**跨越第 k 名边界**
3. 这两个里**恰好一个**是正确 chunk

第 1 条经常成立,1∧2∧3 在 213 个 chunk、60 条查询的规模上一次都没撞上。

**规则**:**「这个机制存在」和「这个机制会改变我的结果」是两个命题,后者要额外估触发概率。**
识别出正确的机制只完成了一半;另一半是问「它需要几个条件同时成立、我的数据规模够不够撞上」。
说「理论上会有影响」是知识,说「但在我这个规模上不触发,因为需要同时满足三个条件」是判断。
**面试追问的第二层几乎总是落在后者。**

对应的方法:预测一个改动的影响时,先写出**触发路径**(哪几步必须都发生),
再对每一步估个数量级;任何一步概率极低,整条路径就可以判为不触发。
(证据强度:受控实验,单一变量,240 个数字全等)

### 70. 换存储时,「id 由谁生成」是第一个要问的问题

**现象**:Qdrant **不接受任意字符串当 point id**,只收 unsigned int 或 UUID。
而 `rag-eval` 的 chunk id 是 `local-01-foundations:05-agent-loop/my_agent.py:44` 这种复合串,
ChromaDB 直接收。解法是 `uuid5(固定 namespace, chunk_id)` 做确定性映射,原串塞进 payload。

**关键在「确定性」三个字**:如果图省事用 `uuid4()`,
每次 reindex 会**插入新点而不是覆盖旧点**,索引里新旧混合——
**和第 8 节那个「不删旧 collection 直接 add」是同一个坑的 Qdrant 版本,而且同样不报错。**

同一次迁移里第二个转换点:**ChromaDB 返回 distance(越小越像),
Qdrant 返回 score(越大越像)**。在 store 内部转成 `1.0 - score`,
上层 `run_eval.py` 的排序逻辑一行没改。

**规则**:**换一个存储后端时,先问三件事,顺序固定:**
① **id 的取值域**——新库允许什么形状的主键?旧库的 id 能不能原样搬?
不能就要映射,而**映射必须是确定性的**,否则「写入」会静默变成「追加」。
② **分数的方向**——是距离还是相似度?越大越好还是越小越好?
方向反了不会报错,只会让排序悄悄倒过来。
③ **哪些调用绕过了抽象层**——`hybrid.py:127` 直接调 `store.client.get_collection().get()`,
那是 Chroma 专有 API,换库必断。**抽象层挡不住绕过它的代码。**

三件里有两件的失败模式是**静默的**——不报错、只是结果不对。所以要主动查,不能等报错。
(证据强度:实测,三件事全部真实遇到)

### 71. 循环的输出必须是数据,不能是 print

**现象**:给 harness 加 SSE 接口时,发现 `agent()` 根本没法接——它一边 `print` 一边跑,
最后 `return` 一个字符串。`print` 送不过 HTTP,而且测试它必须 `contextlib.redirect_stdout` 捕获。

改法是把 `agent()` 拆成两层:`run()` 变成**生成器**,每发生一件事 `yield` 一个带 `type` 的 dict;
`agent()` 退化成一个把事件流排干、只返回最终字符串的薄包装。
**13 条老测试一行没改,全绿。**

```python
def run(goal, provider, ...):          # 生成器
    yield {"type": "tool_use",    "name": ..., "input": ...}
    yield {"type": "tool_result", "content": ..., "is_error": ...}
    yield {"type": "usage",       "input_tokens": ..., "output_tokens": ...}
    yield {"type": "done",        "result": ...}

def agent(...) -> str:                 # 老接口保留
    for event in run(...): ...
```

**规则**:**一个长时间运行的循环,它的中间状态是产品的一部分,不是调试输出。**
`print` 把这些状态锁死在「一个人盯着终端看」这一种消费方式上;
改成结构化事件之后,**同一份数据不加转换层就能进三个地方**:
SSE 帧、JSONL 日志、测试断言。

判断标准很简单:**问「这个 print 的内容,有没有第二个消费者可能想要?」**
对 agent 循环,答案永远是有——前端要显示进度、日志要留痕、评测要看轨迹、埋点要算成本。
所以循环从第一天就该 `yield`,不该 `print`。这个改造越晚做越贵
(要同时改调用方、测试和输出格式)。

**副作用是白拿的**:事件里带 `usage` 之后,token 统计和成本核算不用另起炉灶;
取消、观测埋点、轨迹评测三件事都挂在同一条事件流上。
(证据强度:实测,441 → 774 行,老测试零改动)

### 72. `from X import name` 会被同文件后定义的 `name` 静默覆盖

**现象**:往 `test_loop.py` 里加 3 条取消测试,先是跑出来 **13/13 passed**——
新测试根本没被收集(追加在 `if __name__ == "__main__":` 之后)。挪到前面,又发现更阴的一个:

```python
from loop import run              # 导入循环的生成器
...
def run(goal, responses=None):    # 文件里本来就有的辅助函数, 把导入盖掉了
```

**3 条新测试全部作用在那个辅助函数上,而且会「通过」。**
改成 `from loop import run as loop_run` 之后才现形。

同一轮还踩到第三个:新测试用了 `check(cond, "msg")`,但这个文件用的是裸 `assert`——
`NameError` 报出来了,这个反而是最不危险的。

**规则**:**Python 里后定义覆盖导入,不产生任何警告。**
往一个**已有**的文件里加 import 时,先 `grep` 这个名字在文件里是否已被定义。
同名冲突的失败模式是「**测试通过,但测的是别的东西**」——比报错危险一个量级,
因为报错会停下来,假绿不会。

推广一层:**这一轮三个 bug 里,唯一无害的是那个抛异常的。**
另外两个(测试没被收集、导入被覆盖)的共同特征是**失败时表现为成功**。
所以加完测试的第一件事不是看「过了几条」,是看「**总数变了没有**」——
13 → 16 才说明新测试真的进来了。
(证据强度:实测,同一次改动里三个 bug,两个静默)

### 73. 遥测数据是给聚合器加总的,父层级放「小计」等于多记一笔

**现象**:给 harness 埋 OTel 点,发到 agentevals(本地 OTLP 接收端)。
我实际发出去 `1200 + 1400 = 2600` 个 input token,它报出来 **11400**(另一次是 5200)。

原因是我在**两个层级**用了同一个属性名:

```
【invoke_agent】 gen_ai.usage.input_tokens = 2600   ← 我想写「合计」
  【chat 第1步】 gen_ai.usage.input_tokens = 1200
  【chat 第2步】 gen_ai.usage.input_tokens = 1400
```

后端不看「哪个是父 span」,它**按属性名跨整棵 trace 求和** → `2600+1200+1400`。

同一次还有第二个形态:同一个工具调用我既写进 chat span 的 `gen_ai.output.messages`,
又开了独立的 `execute_tool` span 写 `gen_ai.tool.call.arguments`——
**3 次调用被读成 6 次**。(按 GenAI 规范两处都该写,前者是「模型说它要调」、
后者是「真的跑了」;是这个后端没去重。)

**规则**:**遥测属性是给机器聚合的,不是给人阅读的。**
父层级上放一个「总数 / 小计 / 汇总」,聚合器不会理解成汇总,**只会理解成又一个加数**。
凡是层级汇总,必须换一个后端不认识的属性名(我改成了 `harness.run.input_tokens`)。

推广:**用别人的命名空间,就得接受别人的聚合规则。** `gen_ai.*` 不是一个随便用的前缀,
它是一份契约——名字对上了才能零适配接入现成后端,
但同时也把「这个字段会被怎么处理」的决定权交了出去。自己的东西放自己的命名空间。

**检查方法**:埋完点不要只看自己发出去的 JSON——**发进一个真实消费端,
拿它解析出来的数字和你手算的对一遍**。这次三个 bug(还有一个是最终答案没进
`output.messages`,导致 agentText 为空)**没有一个是读自己代码能发现的**,
我发出去的 JSON 单看毫无问题。
(证据强度:实测,三个 bug 全部由真实消费端暴露)

### 74. 拉(生成器)比推(回调)好接,差别在「谁去找谁要下一个」

**现象**:给循环加埋点时,选了 `instrument(run(...))` 这种生成器套生成器的写法。
我一开始把理由说成「回调会拿走控制权、生成器保住背压」,**实测发现这句是错的**——
同步 Python 里回调也是阻塞的,消费者慢、循环照样等它,两种写法的时间线完全一致。

真正的差别是三条:

| | 生成器(拉) | 回调(推) |
|--|--|--|
| 消费者中途 `break` | **循环立刻停住,不再往下跑** | 循环已经跑完了,拦不住 |
| 中间加一层 | `sanitize(instrument(run()))`,各层互不知道 | 每层都要显式知道下一层是谁 |
| 交给 `StreamingResponse` | 直接给,它要的就是可迭代对象 | 要额外起线程 + 队列把「推」翻译成「拉」 |

第一条对 agent 是钱的问题:**每一步 = 一次模型调用**,客户端断了循环还在跑就是白烧。

**规则**:**一个会持续产出的东西,写成生成器而不是回调。**
判据不是「哪个优雅」,是**下游的形状**:HTTP 流式响应、`for` 循环、测试断言、
管道式的中间层——**全都是「拉」的形状**。写成拉,后面接什么都顺;
写成推,每接一个东西都要写一层适配。

推论(这次的直接收益):埋点层写成透传生成器之后,
**调用方只改了一行** `for e in run(...)` → `for e in instrument(run(...))`,
`agent()` 和 `server.py` 里其余代码一句没动——因为它们拿到的事件和以前完全一样。
**中间站了个人,两头都不知道。**
(证据强度:实测,时间线对比 + 早停行为验证;顺带证伪了我自己关于背压的说法)

### 75. 知道一条规则,和当场用上它,是两回事

**现象**:我写完第 72 条(「往已有测试文件里追加测试,先看**总数**变没变,
因为『没被收集』的失败模式是假绿」)之后**大约一小时**,
往同一个文件追加了 8 条测试,又一次追加到了 `if __name__ == "__main__":` 之后,
跑出来 `16/16 passed` ——**和加之前一模一样**,差点当成过了。
挪到 main 之前才变成 `24/24`。

同一天的另一个形态:新建的埋点文件叫 `trace.py`,**撞了 Python 标准库的 `trace` 模块**。
实测改名前 `import trace` 拿到的是我的文件,标准库被遮蔽,
而且只在用系统 `python3` 跑测试时才炸(那边没有 opentelemetry)。
第 72 条讲的是「同文件内后定义覆盖导入」,这条是「同名文件覆盖标准库」——
**同一个失败模式的两种形态,一天之内踩了两次。**

**规则**:**写下一条规则不会让你对它免疫。** 规则要变成**动作**才有用——
不是「记住要小心命名冲突」,而是**「加完测试,第一件事看总数,不看通过率」**;
不是「注意别撞标准库」,而是**「新建文件时先 `python3 -c "import 文件名"` 试一下」**。

判据:**一条规则如果不能写成一个具体动作(一条命令、一个必须先看的数字),
它就还停在「现象层」,只是听起来像规则。**
这正是这个项目一直在纠的那个毛病,而写规则的人自己也会犯。
(证据强度:实测,同一条规则在写下后一小时内被自己违反两次)

### 76. 文档里的图片引用也是可验证的依赖

**现象**:把外部文章翻译成 Markdown 时,从原页面识别出 **8 张正文图片**,下载到 `docs/assets/` 后逐个检查，8 个相对路径全部存在；图片资源总大小约 **4.0 MB**。页面还包含 hero SVG、站点 logo 和相关文章占位图，如果不区分正文与站点装饰，就会把无关资源一起带进译文。

**规则**:**文档资源不是“顺手复制的附件”,而是需要像代码依赖一样做范围筛选和存在性校验。**先按正文位置区分内容资源与站点资源，再把图片改成本地相对路径；写完后用脚本逐个解析 Markdown 引用并检查文件存在，不能只看预览页面“好像能显示”。

(证据强度:直接实测,8/8 引用通过存在性检查)

### 77. 串行时「自己拼」和「声明 reducer」等价,并行时只有后者能用

**现象**:LangGraph 的 `simple-graph` 里,`State` 是普通 TypedDict、没声明 reducer,
三个 node 都返回 `{"graph_state": ...}`,最终输出 `'Hi, this is Lance. I am sad!'`
**看起来像多段拼接**。但拼接不是框架做的,是 node 自己做的:
`return {"graph_state": state['graph_state'] + " I am"}` —— 先读旧值,交上去的是完整一整句。
**channel 的默认行为是覆盖(last write wins)**,把那个 `+` 去掉,前面的内容就没了。

真正的分水岭在并行。实测(20 行,无需 API key):

```
两个 node 并行写同一个 key,无 reducer:
  InvalidUpdateError: At key 'v': Can receive only one value per step.
                      Use an Annotated key to handle multiple values.
同一个图,加 Annotated[list, operator.add]:
  {'v': ['A', 'B']}
```

**规则**:**「合并策略」是并发的一部分,不是数据结构的一部分。**

串行只有一个写入者,「怎么合」可以藏在写入者内部,自己读旧值再拼就行,看不出缺了什么。
一旦有第二个并发写入者,框架必须在**它俩之外**知道该保留谁——藏在 node 里的拼接逻辑
框架读不到,所以只能报错。reducer 就是把这个策略从写入者手里**外置**到 channel 上。

**推论(不限于 LangGraph)**:凡是「每个写入者自己读-改-写」的结构,
**在单写入者下永远看起来是对的,加并发时一定要重写**。
判据不是「现在有没有 bug」,而是**「如果有两个人同时写这个字段,框架凭什么决定留谁」——
答不上来就说明策略还藏在写入者内部**。

**代价**:外置之后 reducer 会有自己的语义,不能想当然。
`add_messages` 就不是纯 append ——它按 message id 做 upsert(同 id 覆盖)、支持 `RemoveMessage`。
所以「我明明 append 了怎么没变多」不是 bug,是 reducer 在按 id 去重。

对照自己的 harness:`messages` 是在循环里直接 `append`,「怎么合并」散在每个调用点。
现在是单写入者所以没问题,**但第 1 层要做并发工具执行,这一条会正面撞上**。

(证据强度:直接实测,并行无 reducer 稳定抛 `InvalidUpdateError`)

### 78. Notebook 用的是哪个环境,取决于 kernel 的进程树

**现象**:Jupyter Server 及其启动的两个 kernel 都有 `OPENAI_BASE_URL`,但 VS Code 另行启动的
kernel 没有继承这个变量。同一份 `ChatOpenAI(model="deepseek-chat")` 因而把 DeepSeek key
发给了 OpenAI 官方地址,返回 **401 `invalid_api_key`**;把
`base_url="https://api.deepseek.com"` 显式传给构造函数后,检查
`llm.root_client.base_url` 才得到正确地址。

**规则**:**不要从终端或 Jupyter Server 的环境,推断当前 Notebook kernel 的环境。**
IDE、浏览器和命令行可以各自启动不同的 kernel 进程树,同一路径、同一个 venv 也不代表
继承了同一组环境变量。跨提供商使用 OpenAI 兼容接口时,配置是
`model + base_url + api_key` 三元组;第一次请求前应检查客户端解析出的实际 `base_url`,
或在 notebook 中显式传入非敏感的 `base_url`,不要只依赖父进程注入。

(证据强度:直接实测,三个 kernel 的进程环境对照 + 401 错误格式)

### 79. API key 有效不等于 API endpoint 选对了

**现象**:一个格式正确的 LangSmith PAT 请求默认 GCP US endpoint 时,写 trace 和读取 project
都返回 **403 Forbidden**;换 EU 和 AWS US 仍失败,换成
`https://apac.api.smith.langchain.com` 后认证成功,并能在 `langchain-academy` project 中
读到刚写入的 `langsmith-config-smoke` trace。

**规则**:**凭证和服务区域是一个配置对,不能只校验 key。** SaaS 按区域隔离数据时,
同一个 SDK 会默认连接某一区域;区域选错的症状可能只是 403,看起来和 key 无效完全一样。
配置第三方可观测后端时至少验证三件事:`API key + endpoint + 写后读回`。只看环境变量存在,
或者只看到上传代码没有抛错,都不能证明链路已接通。

(证据强度:直接实测,四个区域 endpoint 对照 + trace 写后读回)

### 80. `thread_id` 是地址,checkpointer 才是记忆

**现象**:同一个 Agent 图只替换 checkpointer:`MemorySaver` 能让相同 `thread_id` 的多轮调用
接上历史,但数据只在当前 kernel 内;换成磁盘 `SqliteSaver` 后,
`state_db/local-example.db` 中实际写入了 **9 条 checkpoint**,进程重启后只要重新连接同一文件、
重新编译图,就能按同一个 `thread_id` 读取。换数据库或使用 `:memory:` 都读不到。

**规则**:**标识符只负责定位,存储介质才决定持久性。** 回答「有没有记忆」不能只看
`thread_id`/`session_id` 是否相同,必须同时问「状态存在哪里、对象销毁后还在不在」。
恢复测试必须跨过真实故障边界:内存方案至少重建对象,持久化方案至少重启进程并重连同一存储。

对照自己的 harness:LangGraph 的路线是每 step 保存图状态 snapshot;
计划中的 harness 路线是 intent/result 事件对 + 预分配 id。前者恢复控制流方便,
后者不需要决定快照时机,但要求副作用能靠 id 判定是否已完成。

(证据强度:直接实测,SQLite 文件中按 thread 统计到 9 条 checkpoint)

### 81. 模型看到的上下文不等于系统保存的状态

**现象**:过滤节点只执行 `llm.invoke(state["messages"][-1:])` 时,模型请求只包含最后一条消息,
但图的最终 state 仍保留完整历史;使用 `RemoveMessage` 经 `add_messages` reducer 合并后,
对应消息才真正从 state 消失。`trim_messages` 同样可以只裁剪本轮模型输入,不必删除存储状态。

**规则**:**持久化状态和 provider context 是两个接口。** 过滤/裁剪发送给模型的视图,
解决的是 token 成本和上下文窗口;修改 reducer 返回值,改变的才是后续步骤能够恢复的数据。
把两者绑在一起会让「省 token」意外变成「永久失忆」。设计时应分别回答:
系统保存什么,以及这一轮允许模型看什么。

(证据强度:直接实测,过滤前后 state 消息数不变,模型 trace 只含末条消息)
